- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.vim: vim
    ~/.zshrc

- create:
    - ~/Code

- shell:
  # - [git submodule update --init --recursive, Installing submodules]
  - sleep 2
  - 
    command: ./test.zsh
    stdout: true
    stderr: true
  
